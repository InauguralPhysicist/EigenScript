# Factorial function in EigenScript
# Demonstrates recursive function definition

define factorial as:
    if n is 0:
        return 1
    else:
        prev is n of subtract of 1
        sub_result is factorial of prev
        return n of multiply of sub_result

# Calculate factorial of 5
result is factorial of 5
print of result  # Should output: 120
